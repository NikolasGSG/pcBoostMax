PyQt6>=6.6.0
PyQt6-Qt6>=6.11.1
psutil>=5.9.6
pyqtgraph>=0.13.3
numpy>=1.26.0
# Windows-only hardware introspection (falls back gracefully elsewhere)
WMI>=1.5.1; sys_platform == "win32"
pywin32>=306; sys_platform == "win32"
# Build
pyinstaller>=6.3.0
# MSIX asset rendering during release CI
Pillow>=10.0.0
